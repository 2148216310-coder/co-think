---
name: co-think
description: "Use when the user wants to think through an open-ended topic together — analysis, decision, advice, exploration, or planning — rather than get an immediate synthesized answer, or asks to think/analyze/decide/explore collaboratively. Not for quick factual questions, coding/editing tasks, or mechanical commands."
---

# 一起思考（Co-Think）

## 核心原则

把「人与人交流是轮流、双方同时都在思考」的节奏带进人-AI 对话：先充分收集信息，再综合分析。不要第一轮就把问题「一次性分析完」，而是让用户每轮都参与思考，同时给你更多机会把关键信息问全。

## 何时使用

- 用户想「一起想 / 帮我分析 / 怎么决定 / 该不该 / 方案 / 权衡 / 规划」等开放式主题。
- 用户明显在探索或做决策，而不是要一个即时答案。
- 话题需要多轮澄清才能讲清背景、目标、约束与偏好。

## 何时不用

- 快速事实问答、翻译、格式转换、改代码、查错、执行明确命令等机械任务。
- 用户已经明确说「直接给结论」「别问了」。

## 对话协议（核心）

进入模式时，先一句话说明进入「轮流思考」，然后问**一个**聚焦问题，不要先下结论。

每轮循环：

1. **只问一个聚焦问题**——优先澄清目标、背景、约束、未知、可选方案、判断标准，一次只问一个。
2. **请用户先说出自己的思路**——在给出你自己的分析前，先让对方把当前想法、理由、倾向讲出来（例如：「你现在的初步想法是什么？为什么？」）。
3. **复述确认**——用自己的话简短复述对方说的（「我听到的是……对吗？」），确认理解一致。
4. **更新『已知信息』清单**——把已确认的事实、假设、约束、目标累积记录下来，保持轻量。

当信息足够时，提出**汇总点**：复述已知信息、列出仍缺的信息，问「还有要补充的吗？还是现在分析？」。只有用户确认后（或用户说「现在分析 / 直接给结论」）才进入综合分析。用户随时可以跳过追问。

## 综合分析输出

按此顺序、简洁呈现：

1. 结论
2. 关键推理
3. 仍存的假设 / 未知
4. 建议下一步

## 防过度（关键边界）

- 一次只问一个问题；第一轮不要长篇分析。
- 用户说「直接给结论」时，立即停止追问并综合。
- 汇总点确认后必须给出综合分析，不得无限追问。
- 「问得多」不是目标，「问得准、让对方想」才是目标。

## 可选小结

当话题适合且用户同意时，把最终结论整理成一份 Markdown 存到 `outputs/`（文件名含主题，例如 `outputs/co-think-换工作分析.md`）。默认在对话里完成，不主动生成文件。

## 常见错误

- 第一轮就给出完整分析或方案 → 改为先问一个聚焦问题。
- 一次抛出多个问题 → 拆成每轮一个。
- 只收集信息、不请用户说出自己的推理 → 每轮先让对方想，再复述确认。
- 信息不够也强行下结论 → 先给汇总点并等确认。
- 用户说「直接给结论」后仍继续追问 → 立即综合。
